#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_@nondanee_unblockneteasemusic@0.19.3@@nondanee/unblockneteasemusic/app.js" "$@"
  ret=$?
else 
  node  "$basedir/../_@nondanee_unblockneteasemusic@0.19.3@@nondanee/unblockneteasemusic/app.js" "$@"
  ret=$?
fi
exit $ret
